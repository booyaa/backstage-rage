apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.appName | dump }}
  backstage.io/kubernetes-id: ${{ values.appName | dump }}
  backstage.io/namespace: ${{ values.envName | dump }}
  tags:
    - ${{ values.envName | dump }}
    - yaml
spec:
  type: service
  owner: guests
  lifecycle: experimental
  system: examples

